import { gh, githubConfigured, repo, branch } from '../_lib/github.js';
export default async function handler(req,res){
 if(req.method==='OPTIONS')return res.status(204).end();
 if(req.method!=='POST')return res.status(405).json({ok:false,error:'Method Not Allowed'});
 if(!githubConfigured())return res.status(503).json({ok:false,configured:false,error:'GITHUB_TOKEN is not configured'});
 try{const {message,files,commitBranch=branch}=req.body||{};if(!message||!Array.isArray(files)||!files.length)return res.status(400).json({ok:false,error:'message and files are required'});const ref=await gh(`/git/ref/heads/${encodeURIComponent(commitBranch)}`);const parent=ref.object.sha;const parentCommit=await gh(`/git/commits/${parent}`);const entries=[];for(const f of files){if(!f?.path||typeof f.content!=='string')continue;const blob=await gh('/git/blobs',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({content:f.content,encoding:'utf-8'})});entries.push({path:f.path,mode:'100644',type:'blob',sha:blob.sha})}if(!entries.length)return res.status(400).json({ok:false,error:'no valid files'});const tree=await gh('/git/trees',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({base_tree:parentCommit.tree.sha,tree:entries})});const commit=await gh('/git/commits',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message,tree:tree.sha,parents:[parent]})});await gh(`/git/refs/heads/${encodeURIComponent(commitBranch)}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({sha:commit.sha,force:false})});return res.status(201).json({ok:true,repo,branch:commitBranch,sha:commit.sha,message})}catch(e){return res.status(502).json({ok:false,error:e.message})}
}
